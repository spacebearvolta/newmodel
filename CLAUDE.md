# Working notes for this repo

- Before presenting any rendered/visual output (artifacts, screenshots, previews) as done, actually verify it myself first — render it and check the result, don't just trust that a transformation (e.g. inlining CSS/JS into a single file) worked. A past session shipped a corrupted-CSS artifact without checking and it looked nothing like the real app.
