# Changes — July 14, 2026

Branch: `claude/hook-patterns-touchpoints-vr1gh8`

## Recording / 45-min cap (H1)
- Removed the web modal. The live recording page now matches the desktop app: an inline cut-off box sits below the transcript (centered on web).
- The cut-off box **and** the web capture bar turn **amber** when the 45-min limit is hit.
- Hook gallery updated to show the inline box for the recording state.

## Meetings page
- **Up-next join button:** removed the A/B/C/D cycling; now a single style — green-50 background, brand-700 icon, no border/stroke.
- **3-dots row menu (new):** clicking the ⋮ opens a full context menu (Copy link, Share, Add to playlist, Open in Claude, Open in ChatGPT, Copy transcript for AI, Download transcript for AI, Download transcript, Delete meeting).
  - **Copy link** fires a "Recording link copied" toast for **all** users.
  - **Share** opens the correct modal by state (view-only for free, Business share for an active workspace).
  - For **free users**, only Copy link + Share are active; a green "Try Grain Business · 14 day trial" upsell row sits above "Open in Claude"; Delete meeting stays active.

## History banner (H2)
- Banner outline now matches the Connect-Claude MCP banner (border + drop shadow).

## Trial / upgrade surfaces
- **"Learn more"** on upgrade gates now opens the start-trial value-prop screen.
- **Start-trial value-prop screen redesigned:** header "Try free for 14 days", normal-case "Grain Business" eyebrow, removed the hero box and divider lines, added a "Works with" logo bank (real HubSpot / Salesforce / Slack / Zapier logos, uniform height). Added to the hook gallery with a deep-link.
- **Gradient consistency:** the trial modal, the Plans/pricing Business tier, and the "Upgrade your plan" (UpgradeModal) Business tier now all use the same mint radial anchored in the **bottom-right corner** — fixing the green-at-top gradient that was on the real upgrade modal.

## Grain background colors
- Page `#FFFFFF`, sidebar + up-next `#FAFAFA`, sidebar selection `#F0F0F0` — matched to the live app.

## Earlier today (foundational)
- Toasts: more bottom padding; shown independently of the teammate nudge; right-corner placement, feature-specific green icons.
- Share model: view-only link modal (never gated) + collaboration handoff; single-panel invite with multi-email chips; Business "share with a message" modal (no trial gate).
- Terminology audit: "team" in general UI, "organization" reserved for admin / settings / pricing / org-creation.
- New live meeting-recording surface (`/live`), web + desktop chrome.

## Docs
- `COPY-PASS-TEMPLATE.md` — copy-review worksheet for the whole hook set (every hook × state, copy fields + conditional rules). Created and kept current with the above.
